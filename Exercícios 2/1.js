let n = Number(prompt("Quantas pessoas serão analizadas ?"))

for (let contador = 0; contador < N; contador ++) {
}

if (idade >= 18) {
    console.log(`${nome} pode tirar a CNH`);
} else {
    console.log(`${nome} não pode tirar a CNH`)
}